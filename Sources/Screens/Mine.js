import { StyleSheet, View } from 'react-native';
import { RNContainer, RNHeader } from '../Common';

const Mine = () => {
  return (
    <RNContainer>
      <RNHeader title={'Mine'} back={false}></RNHeader>
    </RNContainer>
  );
};

export default Mine;

const styles = StyleSheet.create({});
