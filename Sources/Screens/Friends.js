import { StyleSheet, View } from 'react-native';
import { RNContainer, RNHeader } from '../Common';

const Friends = () => {
  return (
    <RNContainer>
      <RNHeader title={'Friends'} back={false}></RNHeader>
    </RNContainer>
  );
};

export default Friends;

const styles = StyleSheet.create({});
