import { StyleSheet, View } from 'react-native';
import { RNContainer, RNHeader } from '../Common';

const Earn = () => {
  return (
    <RNContainer>
      <RNHeader title={'Earn'} back={false}></RNHeader>
    </RNContainer>
  );
};

export default Earn;

const styles = StyleSheet.create({});
