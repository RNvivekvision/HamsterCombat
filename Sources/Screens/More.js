import { StyleSheet, View } from 'react-native';
import { RNContainer, RNHeader } from '../Common';

const More = () => {
  return (
    <RNContainer>
      <RNHeader title={'More'} back={false}></RNHeader>
    </RNContainer>
  );
};

export default More;

const styles = StyleSheet.create({});
